grafana:
  grafana.ini:
    server:
      root_url: "%(protocol)s://%(domain)s/grafana/"
      serve_from_sub_path: true

prometheus:
  prometheusSpec:
    externalUrl: http://<YOUR-HAPROXY-IP>/prometheus
    routePrefix: /prometheus